import { AgeUnitFullPipe } from './age-unit-full-pipe';

describe('AgeUnitFullPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeUnitFullPipe();
    expect(pipe).toBeTruthy();
  });
});
